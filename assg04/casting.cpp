// Polygon -> Triangle & Rectangle
#include <cmath>
#include <memory>
Rectangle *rect = Rectangle R(lowerLeft, upperRight);

static_cast<Triangle *>(rect);
// reinterpret_cast<Triangle*>(rect);

dynamic_cast<Triangle *>(rect);
